// import { LocalEntity } from "../domain/entities"

// const getLocalJsonEntity = ( data: string ) => {
//   return data.find( ( local: any ) => local.id === id ) as LocalEntity
// }

// eslint-disable-next-line no-empty
export {}